# widgeta.app
#
# Everything here is meant to be found — that is the product. The only
# exclusions are the application, which is a private tool rather than
# content, and the two legal drafts, which also carry a noindex tag.

User-agent: *
Allow: /

Disallow: /privacy
Disallow: /terms

# One sitemap per language, so indexation can be read per language rather
# than as a single mixed number.
Sitemap: https://widgeta.app/sitemap-index.xml
Sitemap: https://widgeta.app/sitemap-ar.xml
